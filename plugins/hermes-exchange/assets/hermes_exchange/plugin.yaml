name: hermes-exchange
version: 0.1.0
description: "Lightweight Telegram notification relay for allowlisted agent peers."
author: df-haha
kind: standalone
platforms:
  - linux
provides_tools:
  - relay_notify
  - relay_execute
provides_hooks:
  - pre_gateway_dispatch
requires_env:
  - TELEGRAM_BOT_TOKEN
